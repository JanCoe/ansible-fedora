- name: Install development tools
  become: true
  ansible.builtin.dnf:
    name: "{{ dev_tools }}"
    state: present

- name: Enable wezterm Copr repository
  become: true
  ansible.builtin.command: dnf copr enable wezfurlong/wezterm-nightly -y

- name: Enable yazi Copr repository
  become: true
  ansible.builtin.command: dnf copr enable lihaohong/yazi -y

- name: Add nushell repo using template
  become: true
  ansible.builtin.template:
    src: yum.repo.j2
    dest: /etc/yum.repos.d/fury-nushell.repo
    mode: '0644'
  vars:
    repo_id: gemfury-nushell
    repo_name: Gemfury Nushell Repo
    baseurl: https://yum.fury.io/nushell/
    gpgcheck: 0
    gpgkey: https://yum.fury.io/nushell/gpg.key

- name: Install CLI tools
  become: true
  ansible.builtin.dnf:
    name: "{{ cli_tools }}"
    state: present

- name: Install PyCharm as a flatpak
  become: true
  community.general.flatpak:
    name: com.jetbrains.PyCharm-Professional
    state: present
    method: system 

# - name: Punch a hole in the sandbox to connect to dotfiles
#   ansible.builtin.command: flatpak override --user --filesystem=home --env=SHELL=/usr/bin/bash --env=TERM=xterm-256color com.jetbrains.PyCharm-Professional
